#pragma once
#include "Loopie/Math/MathTypes.h"

namespace Loopie
{
	namespace Color
	{	
		const vec4 RED = vec4(255, 0, 0, 255);
		const vec4 GREEN = vec4(0, 255, 0, 255);
		const vec4 BLUE = vec4(0, 0, 255, 255);
		const vec4 WHITE = vec4(255, 255, 255, 255);
		const vec4 BLACK = vec4(0, 0, 0, 0);
		const vec4 GREY = vec4(153, 153, 153, 255);
		const vec4 ORANGE = vec4(255, 128, 0, 255);
		const vec4 YELLOW = vec4(255, 255, 0, 255);
		const vec4 LIME = vec4(128, 255, 0, 255);
		const vec4 TEAL = vec4(0, 255, 128, 255);
		const vec4 SKY_BLUE = vec4(0, 255, 255, 255);
		const vec4 LIGHT_BLUE = vec4(0, 128, 255, 255);
		const vec4 PURPLE = vec4(127, 0, 255, 255);
		const vec4 MAGENTA = vec4(255, 0, 255, 255);
		const vec4 FUCSIA = vec4(255, 0, 127, 255);
	}
}